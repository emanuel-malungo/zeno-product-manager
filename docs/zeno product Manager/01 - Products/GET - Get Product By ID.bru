meta {
  name: GET - Get Product By ID
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/products/by-id?id={{PRODUCT_ID}}&user={{USER_ID}}
  body: none
  auth: inherit
}

params:query {
  id: {{PRODUCT_ID}}
  user: {{USER_ID}}
}
